/**
 * Shared CSS class constants for form inputs.
 * Centralizes Tailwind strings that were previously repeated across modals and pages.
 */

export const INPUT_CLASS =
    'w-full px-4 py-2 bg-gray-900/50 border border-gray-600 rounded-lg text-gray-100 focus:ring-2 focus:ring-indigo-500 focus:border-transparent outline-none transition-all';

export const SELECT_CLASS = INPUT_CLASS;
